#ifndef FEATURES_INCLUDED
#define FEATURES_INCLUDED

#define DEBUG_ALLOC // memory leak detection by tracking all allocations in a datastructure
#define SYNCHRONIZED // adds a critical section around most datastructures

#endif